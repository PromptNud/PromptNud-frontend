# LIFF Configuration
NEXT_PUBLIC_LIFF_ID=your_liff_id
NEXT_PUBLIC_LINE_CHANNEL_ID=your_line_channel_id

# API Configuration
# Local dev: point directly to backend
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1
# Vercel production: use /backend (proxied via next.config.ts rewrites)
# NEXT_PUBLIC_API_BASE_URL=/backend

# Server-side proxy target (Vercel only; unset locally = no proxy)
# API_BASE_URL=https://your-backend-host.com

# Google OAuth (optional)
# NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
